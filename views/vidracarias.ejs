<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Vidrarias de Química</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #2c3e50;
            --secondary: #34495e;
            --success: #27ae60;
            --warning: #f39c12;
            --info: #3498db;
            --danger: #e74c3c;
            --light: #f8f9fa;
            --dark: #2c3e50;
            --gray: #6c757d;
            --light-gray: #e9ecef;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f7fb;
            color: #333;
            min-height: 100vh;
        }
        
        .main-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 1.5rem;
        }
        
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }
        
        .section-title {
            font-size: 1.75rem;
            font-weight: 700;
            color: #2d3748;
        }
        
        .card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            margin-bottom: 2rem;
            transition: all 0.3s ease;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
        }
        
        .card-header {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid #e2e8f0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
        }
        
        .card-title {
            font-size: 1.25rem;
            font-weight: 600;
            color: #2d3748;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .primary-btn {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.7rem 1.2rem;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(78, 84, 200, 0.2);
            text-decoration: none;
        }
        
        .primary-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(78, 84, 200, 0.3);
        }
        
        .danger-btn {
            background: linear-gradient(135deg, var(--danger) 0%, #c0392b 100%);
            color: white;
            border: none;
            border-radius: 8px;
            padding: 0.7rem 1.2rem;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(231, 76, 60, 0.2);
        }
        
        .danger-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(231, 76, 60, 0.3);
        }
        
        .form-control, .form-select {
            padding: 0.75rem 1rem;
            border-radius: 8px;
            margin-bottom: 1.2rem;
            border: 1px solid #d1d3e2;
            width: 100%;
        }
        
        .form-label {
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 0.5rem;
            display: block;
        }
        
        .required-field::after {
            content: " *";
            color: var(--danger);
        }
        
        .input-group {
            display: flex;
        }
        
        .input-group .form-control {
            margin-bottom: 0;
            border-top-right-radius: 0;
            border-bottom-right-radius: 0;
        }
        
        .input-group-text {
            background-color: #f8f9fa;
            border: 1px solid #d1d3e2;
            border-left: none;
            padding: 0.75rem 1rem;
            border-top-right-radius: 8px;
            border-bottom-right-radius: 8px;
            color: #6c757d;
        }
        
        .alert {
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1rem;
        }
        
        .alert-warning {
            background-color: rgba(255, 193, 7, 0.15);
            border-left: 4px solid #ffc107;
            color: #856404;
        }
        
        .movement-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .movement-item {
            display: flex;
            align-items: center;
            padding: 1rem;
            border-bottom: 1px solid #e3e6f0;
            transition: background-color 0.3s;
        }
        
        .movement-item:hover {
            background-color: #f8f9fc;
        }
        
        .movement-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            flex-shrink: 0;
        }
        
        .movement-in {
            background-color: rgba(52, 168, 83, 0.2);
            color: #28a745;
        }
        
        .movement-out {
            background-color: rgba(231, 74, 59, 0.2);
            color: #e74c3c;
        }
        
        .movement-content {
            flex-grow: 1;
        }
        
        .movement-title {
            font-weight: 600;
            margin: 0 0 0.25rem 0;
            color: var(--dark);
        }
        
        .movement-details {
            display: flex;
            gap: 15px;
        }
        
        .movement-quantity, .movement-date {
            font-size: 0.85rem;
            color: #6e707e;
        }
        
        .stats-info {
            font-size: 0.85rem;
            color: #6c757d;
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .view-all-btn {
            background: transparent;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 0.6rem 1.2rem;
            font-size: 0.9rem;
            color: var(--primary);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
        }
        
        .view-all-btn:hover {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(67, 97, 238, 0.2);
        }
        
        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
        }
        
        .toast {
            margin-bottom: 10px;
        }
        
        .glassware-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 20px;
            padding: 1.5rem;
        }
        
        .glassware-card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
            transition: all 0.3s ease;
        }
        
        .glassware-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12);
        }
        
        .glassware-image {
            height: 180px;
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .glassware-image i {
            font-size: 4rem;
            color: var(--primary);
        }
        
        .glassware-content {
            padding: 1.5rem;
        }
        
        .glassware-name {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: var(--dark);
        }
        
        .glassware-details {
            margin-bottom: 1rem;
        }
        
        .glassware-detail {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }
        
        .glassware-detail span:first-child {
            font-weight: 600;
            color: var(--gray);
        }
        
        .glassware-status {
            display: inline-block;
            padding: 0.35rem 0.75rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 600;
        }
        
        .status-available {
            background-color: rgba(52, 168, 83, 0.15);
            color: #28a745;
        }
        
        .status-low {
            background-color: rgba(255, 193, 7, 0.15);
            color: #ffc107;
        }
        
        .status-unavailable {
            background-color: rgba(231, 74, 59, 0.15);
            color: #e74c3c;
        }
        
        .search-container {
            padding: 1rem 1.5rem;
            background-color: white;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .search-input {
            width: 100%;
            padding: 0.75rem 1rem;
            border-radius: 8px;
            border: 1px solid #d1d3e2;
        }
        
        @media (max-width: 768px) {
            .main-container {
                padding: 1rem;
            }
            
            .section-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            
            .card-header {
                flex-direction: column;
                gap: 1rem;
                align-items: flex-start;
            }
            
            .movement-details {
                flex-direction: column;
                gap: 5px;
            }
            
            .glassware-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Conteúdo Principal -->
    <main class="flex-1 overflow-auto bg-gray-50 min-h-screen">
        <div class="main-container">
            <!-- Header da Página -->
            <div class="section-header">
                <h1 class="section-title">
                    <i class="fas fa-flask text-primary mr-2"></i>
                    Vidrarias de Química em Estoque
                </h1>
                <button class="primary-btn" id="add-glassware">
                    <i class="fas fa-plus"></i> Adicionar Vidraria
                </button>
            </div>
            
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- Lista de Vidrarias -->
                <div class="lg:col-span-2">
                    <div class="card">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-vial"></i>
                                Vidrarias Disponíveis
                            </h2>
                            <div class="view-all-btn">
                                <i class="fas fa-filter"></i> Filtrar
                            </div>
                        </div>
                        
                        <div class="search-container">
                            <input type="text" class="search-input" placeholder="Buscar vidraria...">
                        </div>
                        
                        <div class="glassware-grid" id="glassware-list">
                            <!-- As vidrarias serão carregadas via JavaScript -->
                        </div>
                    </div>
                </div>
                
                <!-- Estatísticas e Informações -->
                <div>
                    <div class="card">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-chart-pie text-success"></i>
                                Estatísticas do Estoque
                            </h2>
                        </div>
                        <div class="p-6">
                            <div class="grid grid-cols-2 text-center mb-4">
                                <div class="border-r">
                                    <h4 class="text-primary text-2xl font-bold" id="total-glassware">42</h4>
                                    <p class="text-muted mb-0 text-sm">Total de vidrarias</p>
                                </div>
                                <div>
                                    <h4 class="text-success text-2xl font-bold" id="available-glassware">35</h4>
                                    <p class="text-muted mb-0 text-sm">Disponíveis</p>
                                </div>
                            </div>
                            
                            <div class="mt-4">
                                <h6 class="font-semibold mb-3">Categorias</h6>
                                <div id="category-stats">
                                    <!-- As estatísticas serão carregadas via JavaScript -->
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-info-circle text-info"></i>
                                Informações Importantes
                            </h2>
                        </div>
                        <div class="p-6">
                            <div class="alert alert-warning">
                                <h6 class="font-semibold"><i class="fas fa-exclamation-triangle me-2"></i>Cuidados com vidrarias:</h6>
                                <ul class="mb-0 mt-2 ps-4">
                                    <li>Manusear com atenção para evitar quebra</li>
                                    <li>Verificar trincas ou rachaduras antes do uso</li>
                                    <li>Limpar adequadamente após cada uso</li>
                                </ul>
                            </div>
                            
                            <div class="flex items-center mt-4">
                                <div class="flex-shrink-0 bg-primary bg-opacity-10 p-3 rounded">
                                    <i class="fas fa-boxes fa-lg text-primary"></i>
                                </div>
                                <div class="flex-grow-1 ms-3">
                                    <h6 class="font-semibold mb-0">Estoque Total</h6>
                                    <p class="text-muted mb-0" id="total-items">184 itens em inventário</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-header">
                            <h2 class="card-title">
                                <i class="fas fa-exchange-alt text-warning"></i>
                                Movimentações Recentes
                            </h2>
                        </div>
                        
                        <ul class="movement-list" id="movements-list">
                            <!-- As movimentações serão carregadas via JavaScript -->
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <!-- Container para toasts (notificações) -->
    <div class="toast-container"></div>

    <script>
        // Dados de exemplo para as vidrarias
        const glasswareData = [
            {
                id: 1,
                name: "Balão Volumétrico",
                quantity: 12,
                capacity: "250 mL",
                material: "Vidro borossilicato",
                status: "available",
                category: "Volumétrico"
            },
            {
                id: 2,
                name: "Béquer",
                quantity: 25,
                capacity: "500 mL",
                material: "Vidro temperado",
                status: "available",
                category: "Utilidade geral"
            },
            {
                id: 3,
                name: "Erlenmeyer",
                quantity: 18,
                capacity: "250 mL",
                material: "Vidro borossilicato",
                status: "available",
                category: "Aquecimento"
            },
            {
                id: 4,
                name: "Pipeta Graduada",
                quantity: 3,
                capacity: "10 mL",
                material: "Vidro borossilicato",
                status: "low",
                category: "Medição"
            },
            {
                id: 5,
                name: "Bureta",
                quantity: 8,
                capacity: "50 mL",
                material: "Vidro borossilicato",
                status: "available",
                category: "Titulação"
            },
            {
                id: 6,
                name: "Condensador de Liebig",
                quantity: 0,
                capacity: "300 mm",
                material: "Vidro borossilicato",
                status: "unavailable",
                category: "Destilação"
            },
            {
                id: 7,
                name: "Funil de Separação",
                quantity: 5,
                capacity: "250 mL",
                material: "Vidro borossilicato",
                status: "available",
                category: "Separação"
            },
            {
                id: 8,
                name: "Proveta",
                quantity: 2,
                capacity: "100 mL",
                material: "Vidro borossilicato",
                status: "low",
                category: "Medição"
            }
        ];

        // Dados de exemplo para movimentações
        const movementData = [
            {
                id: 1,
                type: "out",
                glassware: "Béquer 500mL",
                quantity: 5,
                date: "2023-06-15T10:30:00",
                responsible: "Dr. Silva"
            },
            {
                id: 2,
                type: "in",
                glassware: "Pipeta Graduada 10mL",
                quantity: 10,
                date: "2023-06-14T16:45:00",
                responsible: "Lab. Central"
            },
            {
                id: 3,
                type: "out",
                glassware: "Erlenmeyer 250mL",
                quantity: 8,
                date: "2023-06-14T09:15:00",
                responsible: "Profa. Oliveira"
            },
            {
                id: 4,
                type: "in",
                glassware: "Balão Volumétrico 250mL",
                quantity: 6,
                date: "2023-06-13T14:20:00",
                responsible: "Fornecedor XYZ"
            }
        ];

        // Função para exibir notificações
        function showToast(message, type = 'success') {
            const toastContainer = document.querySelector('.toast-container');
            const toastId = 'toast-' + Date.now();
            
            const toast = document.createElement('div');
            toast.className = `toast align-items-center text-bg-${type} border-0`;
            toast.id = toastId;
            toast.setAttribute('role', 'alert');
            toast.setAttribute('aria-live', 'assertive');
            toast.setAttribute('aria-atomic', 'true');
            
            toast.innerHTML = `
                <div class="d-flex">
                    <div class="toast-body">
                        ${message}
                    </div>
                    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                </div>
            `;
            
            toastContainer.appendChild(toast);
            
            // Remover o toast após 5 segundos
            setTimeout(() => {
                if (toast.parentNode) {
                    toast.parentNode.removeChild(toast);
                }
            }, 5000);
        }

        // Função para carregar as vidrarias
        function loadGlassware() {
            const glasswareList = document.getElementById('glassware-list');
            glasswareList.innerHTML = '';
            
            glasswareData.forEach(glassware => {
                const glasswareCard = document.createElement('div');
                glasswareCard.className = 'glassware-card';
                
                // Determinar ícone com base no tipo de vidraria
                let iconClass = 'fas fa-flask'; // padrão
                if (glassware.name.includes('Balão')) iconClass = 'fas fa-weight';
                if (glassware.name.includes('Béquer')) iconClass = 'fas fa-glass-whiskey';
                if (glassware.name.includes('Erlenmeyer')) iconClass = 'fas fa-mortar-pestle';
                if (glassware.name.includes('Pipeta')) iconClass = 'fas fa-tint';
                if (glassware.name.includes('Bureta')) iconClass = 'fas fa-ruler-vertical';
                if (glassware.name.includes('Condensador')) iconClass = 'fas fa-wind';
                if (glassware.name.includes('Funil')) iconClass = 'fas fa-filter';
                if (glassware.name.includes('Proveta')) iconClass = 'fas fa-vial';
                
                // Determinar classe de status
                let statusClass = 'status-available';
                let statusText = 'Disponível';
                if (glassware.status === 'low') {
                    statusClass = 'status-low';
                    statusText = 'Estoque Baixo';
                } else if (glassware.status === 'unavailable') {
                    statusClass = 'status-unavailable';
                    statusText = 'Indisponível';
                }
                
                glasswareCard.innerHTML = `
                    <div class="glassware-image">
                        <i class="${iconClass}"></i>
                    </div>
                    <div class="glassware-content">
                        <h3 class="glassware-name">${glassware.name}</h3>
                        <div class="glassware-details">
                            <div class="glassware-detail">
                                <span>Quantidade:</span>
                                <span>${glassware.quantity} unidades</span>
                            </div>
                            <div class="glassware-detail">
                                <span>Capacidade:</span>
                                <span>${glassware.capacity}</span>
                            </div>
                            <div class="glassware-detail">
                                <span>Material:</span>
                                <span>${glassware.material}</span>
                            </div>
                            <div class="glassware-detail">
                                <span>Categoria:</span>
                                <span>${glassware.category}</span>
                            </div>
                        </div>
                        <span class="glassware-status ${statusClass}">${statusText}</span>
                    </div>
                `;
                
                glasswareList.appendChild(glasswareCard);
            });
        }

        // Função para carregar as movimentações
        function loadMovements() {
            const movementsList = document.getElementById('movements-list');
            movementsList.innerHTML = '';
            
            movementData.forEach(movement => {
                const movementItem = document.createElement('li');
                movementItem.className = 'movement-item';
                
                movementItem.innerHTML = `
                    <div class="movement-icon ${movement.type === 'in' ? 'movement-in' : 'movement-out'}">
                        <i class="fas fa-arrow-${movement.type === 'in' ? 'down' : 'up'}"></i>
                    </div>
                    <div class="movement-content">
                        <p class="movement-title">${movement.type === 'in' ? 'Entrada' : 'Saída'} - ${movement.glassware}</p>
                        <div class="movement-details">
                            <span class="movement-quantity">${movement.quantity} unidades</span>
                            <span class="movement-date">${new Date(movement.date).toLocaleString('pt-BR')}</span>
                        </div>
                        <small>Por: ${movement.responsible}</small>
                    </div>
                `;
                
                movementsList.appendChild(movementItem);
            });
        }

        // Função para carregar estatísticas
        function loadStatistics() {
            // Calcular totais
            const totalGlassware = glasswareData.reduce((sum, item) => sum + item.quantity, 0);
            const availableGlassware = glasswareData.filter(item => item.status === 'available')
                .reduce((sum, item) => sum + item.quantity, 0);
            
            // Atualizar os contadores
            document.getElementById('total-glassware').textContent = totalGlassware;
            document.getElementById('available-glassware').textContent = availableGlassware;
            document.getElementById('total-items').textContent = `${totalGlassware} itens em inventário`;
            
            // Calcular estatísticas por categoria
            const categoryStats = {};
            glasswareData.forEach(item => {
                if (!categoryStats[item.category]) {
                    categoryStats[item.category] = 0;
                }
                categoryStats[item.category] += item.quantity;
            });
            
            // Atualizar as estatísticas de categorias
            const categoryStatsElement = document.getElementById('category-stats');
            categoryStatsElement.innerHTML = '';
            
            for (const category in categoryStats) {
                const percentage = Math.round((categoryStats[category] / totalGlassware) * 100);
                const progressBar = document.createElement('div');
                progressBar.className = 'd-flex align-items-center mt-3';
                
                progressBar.innerHTML = `
                    <div class="flex-grow-1 me-3">
                        <div class="small text-muted">${category}</div>
                        <div class="progress mt-1" style="height: 8px;">
                            <div class="progress-bar bg-primary" role="progressbar" style="width: ${percentage}%" 
                                aria-valuenow="${percentage}" aria-valuemin="0" aria-valuemax="100"></div>
                        </div>
                    </div>
                    <div class="flex-shrink-0">
                        <span class="text-dark fw-bold">${percentage}%</span>
                    </div>
                `;
                
                categoryStatsElement.appendChild(progressBar);
            }
        }

        // Botão para adicionar vidraria
        document.getElementById('add-glassware').addEventListener('click', function() {
            showToast('Funcionalidade de adicionar vidraria em desenvolvimento', 'info');
        });

        // Carregar dados iniciais
        document.addEventListener('DOMContentLoaded', function() {
            loadGlassware();
            loadMovements();
            loadStatistics();
            
            // Simular carregamento de dados
            showToast('Estoque de vidrarias carregado com sucesso!');
        });
    </script>
</body>
</html>